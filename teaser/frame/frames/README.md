# Frames for "The Last Frame"

Upload the seven WebP files into **this folder**, named exactly:

```
frame-01.webp
frame-02.webp
frame-03.webp
frame-04.webp
frame-05.webp
frame-06.webp
frame-07.webp
```

900 × 1600, frame 7 is the empty one. The page (`../index.html`) already
points at these paths — as soon as the files exist it uses them; until then
it falls back to procedural placeholders automatically. Nothing else needs
to change.
